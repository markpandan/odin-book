import { describe, it } from "vitest";

describe("Profile Page", () => {
  it("sees the loading page when fetching data", () => {});

  it("sees the proper data when fetching was finished", () => {});

  it("sees the error status when fetch was rejected", () => {});
});
