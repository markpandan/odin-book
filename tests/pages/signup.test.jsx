import { describe, it } from "vitest";

describe("Signup Page", () => {
  it("should display a loading status when the promise is pending", () => {});

  it("should display an error when the promise was rejected", () => {});

  it("should route to the login page when promise was successful", () => {});
});
